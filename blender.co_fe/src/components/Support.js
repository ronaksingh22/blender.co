import React from 'react';
import PropTypes from 'prop-types';

Support.propTypes = {
    
};

function Support(props) {
    return (
        <div>
            
        </div>
    );
}

export default Support;