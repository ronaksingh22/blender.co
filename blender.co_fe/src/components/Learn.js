import React from 'react';
import PropTypes from 'prop-types';

Learn.propTypes = {
    
};

function Learn(props) {
    return (
        <div>
            
        </div>
    );
}

export default Learn;